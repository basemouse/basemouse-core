node_modules/
.env
.DS_Store
coverage/
*.log
.basemouse/claude-loop/
.gstack/

# Local MCP configs
.mcp.json

# Browser-automation scratch output (QA/dogfooding). Snapshots and screenshots
# capture logged-in state, so these are never shared artifacts. Also excluded
# from the open-core sync via scripts/core-exclude.txt — rsync ignores this
# file, so that listing is the guard that actually holds.
.playwright-mcp/

# Per-developer session log (imported into BaseMouse from disk; not a shared artifact)
/PROGRESS.md

# Private real-corpus eval material. The corpus file is regenerable via
# scripts/export-corpus.mjs; the golden-query files (workspace-projects.json,
# workspace-paraphrase.json) are HAND-AUTHORED and NOT regenerable — back
# them up before deleting anything in this directory.
data/retrieval-eval/local/
